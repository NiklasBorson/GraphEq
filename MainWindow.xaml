<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GraphEq.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GraphEq"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Formula Grapher">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <canvas:CanvasControl 
            x:Name="Canvas" 
            Grid.RowSpan="2" 
            CreateResources="CanvasControl_CreateResources" 
            Draw="CanvasControl_Draw" 
            ManipulationMode="Scale,TranslateX,TranslateY" 
            ManipulationDelta="CanvasControl_ManipulationDelta"
            PointerWheelChanged="CanvasControl_PointerWheelChanged"/>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="f(x) =" VerticalAlignment="Center"/>
            <TextBox x:Name="FormulaTextBox" Width="300" PlaceholderText="Type formula" TextChanged="TextBox_TextChanged"/>
            <Button Click="CenterButton_Click" ToolTipService.ToolTip="Center grid.">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE81D;"/>
            </Button>
        </StackPanel>

        <Button Margin="5" HorizontalAlignment="Right" VerticalAlignment="Top" ToolTipService.ToolTip="Open side panel." Click="OpenSidePanel_Click">
            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A0;"/>
        </Button>

        <Border x:Name="SidePanel"
                Width="300" Grid.RowSpan="2" HorizontalAlignment="Right"
                Background="DarkGray" BorderBrush="DarkSlateGray" BorderThickness="2" Padding="3">
            <Border.Resources>
                <Storyboard x:Name="SidePanelOpenAnimation">
                    <DoubleAnimation 
                        Storyboard.TargetName="SidePanelTranslation"
                        Storyboard.TargetProperty="X"
                        From="300" To="0" Duration="0:0:0.1"/>
                </Storyboard>
                <Storyboard x:Name="SidePanelCloseAnimation">
                    <DoubleAnimation 
                        Storyboard.TargetName="SidePanelTranslation"
                        Storyboard.TargetProperty="X"
                        From="0" To="300" Duration="0:0:0.1"/>
                </Storyboard>
            </Border.Resources>
            <Border.RenderTransform>
                <TranslateTransform x:Name="SidePanelTranslation" X="300"/>
            </Border.RenderTransform>
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button HorizontalAlignment="Right" ToolTipService.ToolTip="Close side panel." Click="CloseSidePanel_Click">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE89F;"/>
                </Button>
                <TabView Grid.Row="1" VerticalAlignment="Stretch" IsAddTabButtonVisible="False">
                    <TabViewItem Header="Help" IsClosable="False">
                        <ScrollViewer>
                            <TextBox IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" Text="{x:Bind HelpText,Mode=OneTime}"/>
                        </ScrollViewer>
                    </TabViewItem>
                    <TabViewItem Header="My Functions" IsClosable="False">
                        <ScrollViewer VerticalAlignment="Stretch">
                            <TextBox 
                                x:Name="UserFunctionsTextBox"
                                IsSpellCheckEnabled="False"
                                AcceptsReturn="True" 
                                TextChanged="UserFunctionsTextBox_TextChanged" 
                                PlaceholderText="Type one function per line, e.g.,&#10;pow(x,y) = x^y"/>
                        </ScrollViewer>
                    </TabViewItem>
                </TabView>
            </Grid>
        </Border>
    </Grid>
</Window>
